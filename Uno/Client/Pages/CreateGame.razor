@page "/"

@using Uno.Client
@using Uno.Shared

@inject Api api
@inject NavigationManager navigationManager

<button class="btn" @onclick="OnClickCreateGame">Create game</button>

@code {
    private async Task OnClickCreateGame()
    {
        var result = await api.UnoGame.CreateGameAsync(new CreateGameRequest());
        if (result.IsSuccess)
        {
            this.navigationManager.NavigateTo($"/uno/{result.GameId}");
        }
    }
}
